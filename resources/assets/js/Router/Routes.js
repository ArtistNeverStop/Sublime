import Site from '@/Components/Sections'
import Route from '@/Router/RouteMaker'

/**
 * The Routes of the app in a
 * Route object.
 * ------------------------------
 * @const {Array}
 */
export default [
  Route('/', Site.Home)
]
